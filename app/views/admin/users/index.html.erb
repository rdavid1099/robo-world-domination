<h1>Admin View All Users</h1>
<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to user.username, admin_user_path(user) %><br>
      <%= link_to "Edit", edit_admin_user_path(user) %> | <%= link_to "Delete", admin_user_path(user), method: :delete %>
    </li>
  <% end %>
</ul>
